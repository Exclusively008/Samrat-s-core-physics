
# TODO: Location selection dropdown on batch pages

- [ ] Update `class9.html` to include a location `<select>` dropdown and re-render time options on change.
- [ ] Update `class10.html` to include a location `<select>` dropdown and re-render time options on change.
- [ ] Update `class11.html` to include a location `<select>` dropdown and re-render time options on change.
- [ ] Update `class12.html` to include a location `<select>` dropdown and re-render time options on change.
- [ ] Verify dropdown reads `?location=` (query string) and falls back to `localStorage.selectedLocation`.
- [ ] Verify WhatsApp purchase uses updated location (via `window.BATCH_META.location`).
- [ ] Manual test: open each class page with/without `?location=` and confirm UI behavior.

