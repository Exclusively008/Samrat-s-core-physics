# TODO

## Location selector removal
- [x] Remove location dropdown + link query appending from `batches.html`.
- [x] Update `class9.html`, `class10.html`, `class11.html`, `class12.html` to handle missing `location`:

  - [ ] show fallback text in `#loc` when no location is provided
  - [ ] disable/hide time choices when location is missing and show a hint
  - [ ] ensure attendance initialization still doesn’t crash
- [ ] Sanity check in browser: navigate from `batches.html` to each class page.
- [ ] Quick smoke-test: verify no console JS errors.

